DATABASE_URL="postgresql://tmpro:tmpro@localhost:5432/tmpro"
JWT_SECRET="dev-only-change-me"
PORT=3001

# Base URL of the web app (apps/web) — used to build links inside emails,
# e.g. the "Reset password" one-time link an Admin/HR sends from a People
# profile. Defaults to the local dev server; set this to your deployed
# frontend's URL in any other environment.
# WEB_APP_URL="http://localhost:3000"

# Email notifications (leave requests/decisions, announcements, employee detail
# changes). Any SMTP-speaking provider works here (Resend, SendGrid, Postmark,
# Mailgun, SES, etc. all offer an SMTP relay) — just point these at it. Leave
# SMTP_HOST unset to use the built-in dev-mode transport, which logs emails to
# the console instead of sending them, so the feature works out of the box with
# no provider configured.
# SMTP_HOST="smtp.example.com"
# SMTP_PORT=587
# SMTP_USER="apikey"
# SMTP_PASS="changeme"
# SMTP_FROM="Talent Notifications <notifications@example.com>"

# Subscription billing (v025.A) — Stripe. Leave STRIPE_SECRET_KEY unset and
# self-serve sign-up / Settings → Billing stay switched off (everything else
# works as before). Use test keys (sk_test_…) until you're ready to go live.
# After setting the key, run `npm run billing:setup` once to create tmPro's
# products, prices and Customer Portal configuration in Stripe.
# STRIPE_SECRET_KEY="sk_test_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."          # from the webhook endpoint you add in Stripe
# STRIPE_PORTAL_CONFIGURATION="bpc_..."      # printed by npm run billing:setup
# BILLING_TRIAL_DAYS=7
